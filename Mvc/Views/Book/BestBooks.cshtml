@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model IEnumerable<BookBriefInformation>
<h1>Best books</h1>
<div class="list-group">
    @foreach (var book in Model)
    {
        <a asp-controller="Book" asp-action="BookFullInformation" asp-route-id="@book.Id" class="list-group-item list-group-item-action">@(book.Title)(@book.Year)(@book.Author)(@book.Genre) - @(@book.SellingPrice)$</a>
    }
</div>
